Возможно ли создать функции A и B в примере ниже, где объекты равны new A()==new B()?
function A() { ... }
function B() { ... }

let a = new A;
let b = new B;

alert( a == b ); // true
=======SOLUTION=========
Не врубился в смысл задания, поэтому посмотрел решение. Разобрался. Я знаю, что объект может быть равен только сам себе, но никак не другим объектам. 
Не подумал, что функции эти могут возаращть один и тот же объект, который был определен снаружи. Тогда будет true конечно. 
