let n = +prompt("Введите число и я скажу, простое оно или нет");
for (let i = 2; i < n; i++) {
    if (n % i == 0) {
        alert(`${n} -  не простое число`);
        break;
    }
    else {
        alert(`${n} -  простое число`);
        break;
    }
}
